import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    borderTopColor: "#808080",
    borderTopWidth: 0.2,
  },
  image: {
    height: 60,
    width: 50,
    marginTop: 45,
    marginBottom: 20,
    resizeMode: "cover",
  },
  textBold: {
    color: "#808080",
    fontWeight: "700",
    fontSize: 16,
  },
  text: {
    color: "#808080",
    fontSize: 16,
  },
});
